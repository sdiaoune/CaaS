CaaS — Remaining Work Checklist

AUTH & ENV — DONE
- Ensure Supabase Auth redirect URLs include http://localhost:3000/app/auth/callback and http://localhost:3001/app/auth/callback
- Add SUPABASE_SERVICE_ROLE_KEY to .env.local for local bootstrap (org + project + default policy)
- Keep magic link redirectTo set to /app/auth/callback (done). Remove old route /app/(public)/auth/callback if still present — DONE
- Middleware allows /app/auth/callback unauthenticated (done). Verify navigation after login always redirects to /app — DONE

ROUTES & FETCH PATHS — DONE
- Changed all browser fetches to /api and added safe JSON parsing guards across all app pages

SOURCES (Acceptance #2)
- Create POST /api/sources/create — DONE
- Add Source modal triggers create and refresh — DONE
- Connect/Sync endpoints persist status and write audit — DONE

DOCUMENTS (Acceptance #3, #4) — DONE
- Ingest endpoints and signed upload — DONE
- Document drawer wired with chunks and preview — DONE
- Re-index/delete actions — DONE

PLAYGROUND (Acceptance #5) — DONE
- /api/answer implemented; UI shows citations and metrics

PIPELINES (Acceptance #8)
- Endpoints (create/update/delete/deploy) — DONE
- Wire UI: Create modal calls create — DONE; Detail modal deploy — DONE; Update/Delete from UI — DONE

EVALUATIONS (Acceptance #7)
- Run button passes evalSetId; runs table wired — DONE
- Eval set create endpoint and modal submit — DONE (stores items in config)

GOVERNANCE (Acceptance #8) — DONE
- Governance page wired to GET/PUT — DONE
- Basic profanity redaction applied in /api/answer — DONE
- Allowed_sources filtering passed through to `search` — DONE (server forwards policy to function)

AUDIT LOGS (Acceptance #9) — DONE
- Server writes for ingest, source connect/sync, policy update, pipeline deploy, eval run
- Pagination and search in UI

DASHBOARD (Acceptance #6) — DONE
- Added APIs: /api/dashboard/docsOverTime, /api/dashboard/bySource, /api/dashboard/evalLatency
- Updated dashboard to render charts and p95 metric

RLS (Acceptance #10) — DONE (validated scripts run)
- `scripts/rls-test.ts` added; run to bootstrap and manually validate isolation with two accounts

EDGE FUNCTIONS & SECRETS — DONE (configured and deployed)
- Ensure function secrets set in Supabase; optional PII extraction later
  - OPENAI_API_KEY, EMBEDDING_MODEL, SUPABASE_URL, SUPABASE_ANON_KEY
  - Optionally enable PII extraction to populate chunks.metadata.pii_flags

TYPEGEN & SEEDING — DONE
- Generate types, run seed after env configured
  - supabase types (e.g., supabase gen types typescript --linked > types/supabase.ts)
  - Seed: pnpm ts-node scripts/seed.ts (after env + edge functions)

READMES & PRELAUNCH — PARTIAL (README scaffold added)
- Update README and add security notes
  - Setup: env, migrate, deploy functions, generate types, seed, start app
  - Security: RLS overview, avoid service role on client, secrets management

BUGS/KNOWN FIXES
- Removed duplicate callback route — DONE
- Ensure SUPABASE_SERVICE_ROLE_KEY present; without it /api/me/project is null — REMINDER
- All pages updated to /api and safe parsing added — DONE
