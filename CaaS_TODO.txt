CaaS — Remaining Work Checklist

AUTH & ENV
- Ensure Supabase Auth redirect URLs include http://localhost:3000/app/auth/callback and http://localhost:3001/app/auth/callback
- Add SUPABASE_SERVICE_ROLE_KEY to .env.local for local bootstrap (org + project + default policy)
- Keep magic link redirectTo set to /app/auth/callback (done). Remove old route /app/(public)/auth/callback if still present
- Middleware allows /app/auth/callback unauthenticated (done). Verify navigation after login always redirects to /app

ROUTES & FETCH PATHS
- Change all browser fetches from /app/api/... to /api/... (App Router exposes /api)
  Files to update:
  - app/app/sources/page.tsx
  - app/app/documents/page.tsx
  - app/app/pipelines/page.tsx
  - app/app/evals/page.tsx
  - app/app/playground/page.tsx
  - app/app/page.tsx
  Done: All updated

SOURCES (Acceptance #2)
- Wire Add Source flow to insert a row in public.sources
  - Create POST /api/sources/create (projectId, type, name, config?)
  - On success, refresh list and enable Connect/Sync
- Ensure Connect button persists status=connected and Sync triggers /api/sources/sync (done) and writes audit_logs
  Done: sources/create + connect body accepts sourceId; sync writes audit

DOCUMENTS (Acceptance #3, #4)
- Add document ingest UI (upload text/file or URL)
  - Call POST /api/ingest with { projectId, title, text | fileUrl | url }
  - For file upload: create POST /api/uploads/createSignedUrl and PUT to Storage bucket 'documents'; pass fileUrl to ingest
- Replace DocumentDrawer mocks
  - New GET /api/documents/[id]/chunks?limit=20 → join chunks by document_id ordered by chunk_index
  - Optionally GET /api/documents/[id] → return raw_text preview and pii_flags
- Documents page: keep pagination (done); add source filter (map source_id to names is done). Add delete and re-index actions
  Implemented: uploads/createSignedUrl, documents/[id], documents/[id]/chunks, DocumentDrawer wired

PLAYGROUND (Acceptance #5)
- Replace stubbed answer with server-side generation
  - New POST /api/answer: { projectId, query, k } → call edge function search, then OpenAI/LLM to synthesize answer using contexts (respect RAG_MAX_TOKENS)
  - Show citations (done), show model latency/token/cost metrics
  Implemented: /api/answer basic synthesis and metrics

PIPELINES (Acceptance #8 partial)
- CRUD endpoints
  - POST /api/pipelines/create, PUT /api/pipelines/[id], DELETE /api/pipelines/[id]
  - Deploy: POST /api/pipelines/[id]/deploy → compute and persist last_deploy_hash; write audit_logs
- Wire UI modals to these endpoints
  Implemented: endpoints added; UI wiring pending

EVALUATIONS (Acceptance #7)
- Create eval items or seed strategy
  - Option A: add table public.eval_items(eval_set_id, question, answer)
  - Option B: store items JSON on eval_sets.config
- Update run button to pass evalSetId (done) and display latest runs table (wired). Add toast on completion

GOVERNANCE (Acceptance #8)
- Implement Governance page bindings to GET/PUT /api/governance (endpoints exist)
- Confirm edge function search applies guardrails (pii_redaction done); extend to allowed_sources and profanity level

AUDIT LOGS (Acceptance #9)
- Ensure audit writes on: source connect/sync, ingest, policy update, pipeline deploy, eval run (ingest/eval done)
- Audit page pagination (done). Add filters by event_type
  Implemented: audit UI fetch with pagination and search; server writes for ingest, source sync, pipeline deploy, answer

DASHBOARD (Acceptance #6)
- Replace mock charts with real queries
  - Indexed docs over time (documents.created_at by day)
  - Ingested by source (join sources)
  - Latency p95 from eval_runs.duration_ms (last N runs)

RLS (Acceptance #10)
- Add scripts/rls-test.ts
  - Create two users (or use two sessions) → verify user A cannot read user B’s projects/documents
  - Use service role only in server routes/edge functions

EDGE FUNCTIONS & SECRETS
- In Supabase dashboard → set OPENAI_API_KEY, EMBEDDING_MODEL, SUPABASE_URL, SUPABASE_ANON_KEY for functions
- Optional: add pii extraction to populate chunks.metadata.pii_flags during ingest

TYPEGEN & SEEDING
- supabase:types script exists; run and commit types/supabase.ts
- Verify scripts/seed.ts after setting envs; ensure it creates demo data and two demo docs

READMES & PRELAUNCH
- Update README with setup (env, MCP/CLI link, migrate, deploy functions, seed, start app)
- Security notes on RLS and avoiding service role on client

BUGS/KNOWN FIXES
- Remove duplicate callback route under app/(public)/auth/callback if keeping app/app/(public)/auth/callback
- Ensure SUPABASE_SERVICE_ROLE_KEY present so bootstrap creates default project; without it /api/me/project returns null causing empty pages
- Verify all pages load data after switching fetches to /api
  Fixed: removed duplicate route under app/app; fetch paths updated
